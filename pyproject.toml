[project]
name = "nimbot-cups"
version = "0.1.0"
description = "CUPS backend and filter for the Nimbot B1 label printer"
requires-python = ">=3.11"
dependencies = [
  "Pillow>=10.0.0",
  "pyserial>=3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
